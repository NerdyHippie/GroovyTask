import { ShowCompletePipe } from './show-complete.pipe';

describe('ShowCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
